package example;

import com.bluzelle.Bluzelle;
import com.bluzelle.GasInfo;
import com.bluzelle.LeaseInfo;

class Data {
    static Bluzelle bluzelle;
    static GasInfo gasInfo = new GasInfo(10, 0, 0);
    static LeaseInfo leaseInfo = new LeaseInfo(0, 0, 10, 0);
}
